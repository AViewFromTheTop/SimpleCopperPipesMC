# Increases the amount of memory Gradle can use and optimizes the GC
	# -XX:+UseZGC
	org.gradle.jvmargs=-Xmx4G
# Helps with build times
	org.gradle.parallel=true
# Caches Dependencies for faster build times.
	org.gradle.caching=false

# Fabric Properties
	# Get new versions at https://fabricmc.net/develop
	minecraft_version=26.2
	fabric_loader_version=0.19.5
	min_fabric_loader_version=0.19.0

# Mod Properties
	mod_id = simple_copper_pipes
	mod_name = Simple Copper Pipes
	mod_author = Lunade/AViewFromTheTop
	mod_version = 2.2
    # protocol version must be changed after each update with dual env changes
	protocol_version = 2
	maven_group = net.lunade
	archives_base_name = SimpleCopperPipes
    subproject_prefix = scp
	java_version = 25
    mod_url = https://modrinth.com/mod/simple-copper-pipes
    source_url = https://github.com/AViewFromTheTop/SimpleCopperPipesMC
    issues_url = https://github.com/AViewFromTheTop/SimpleCopperPipesMC/issues
	license = FrozenBlock Modding Oasis License v1.0
	repository = AViewFromTheTop/SimpleCopperPipesMC

# Publishing
    modrinth_project_id = 9r4ZkgSN
    curseforge_project_id = 571336

# Dependencies
	fabric_api_version=0.160.0+26.2
	frozenlib_version=3.0-mc26.2
    thecopperierage_version=1.3-mc26.2
# External Mods
	modmenu_version=20.0.2
	cloth_config_version=26.2.155

# NeoForge Properties (verify versions against the snapshot maven)
    neoforge_version=26.2.0.88
    neoforge_loader_version_range=[4,)

# Optional: set to a PR/snapshot NeoForge maven URL, or leave blank to skip
    neoforge_snapshot_maven=
